[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
dockerBuildArgs = ["VITE_CLERK_PUBLISHABLE_KEY=${{VITE_CLERK_PUBLISHABLE_KEY}}"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
